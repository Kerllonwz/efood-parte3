import styled from 'styled-components'

export const Grid = styled.div`
  display: grid;
  grid-template-columns: repeat(2, 1fr);
  gap: 48px 56px;
  max-width: 760px;
  margin: 0 auto;
  padding: 48px 0 64px;
`
